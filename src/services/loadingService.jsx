import { ProgressBar } from "react-loader-spinner"

export const LoadingService = () => {
    
        return(
            
                <ProgressBar
                visible={true}
                height="80"
                width="80"
                barColor="#000000"
                borderColor = '#000000'
                />
            
        )   
    
}